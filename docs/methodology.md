# Methodology

The engine scores deployment configurations using latency, energy, cost, accuracy, carbon, and constraint penalties. It includes a real sklearn digits dataset benchmark and optional PyTorch/ONNX timing.
